# 06tinyec2025


## Usage

Iris データセットの散布図を描画するスクリプトを含みます。

- 依存関係をインストール:

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

- スクリプトを実行:

```bash
python plot_iris.py
```

実行すると `iris_scatter.png` を作成し、プロットを表示します。
